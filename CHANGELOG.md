## Changes

### Features
- add changelog generation from conventional commits
- replace text icon with dual-scale badge design
- match bare in for standalone inches with negative lookahead
- add smart/curly quote support for feet and inches patterns
- add esbuild build step for single-source content script
- add extension icons
- add popup UI with enable/disable toggle
- add background service worker with install defaults
- add content script with DOM scanning and MutationObserver
- add text replacement engine combining patterns and conversions
- add regex-based measurement detection with priority ordering
- add conversion logic with unit tests
- initialize project with MV3 manifest

### Bug Fixes
- replace METRIC_PATTERN early return with per-match check, scan full body

### Documentation
- add approved icon design for dual-scale badge

### Chores
- install commit-msg hook for commitlint
- add commitlint for conventional commits enforcement
- **Docs:** Update readme with flag

### Other Changes
- cover convertMeasurement default branch via mock module
- cover convertDimensions2D meters path (lines 39-40)
- Support more metrics (#2)
- Cleanup
- Add command to compact
- Update
- Add license
- Update metadata
- First commit

---

