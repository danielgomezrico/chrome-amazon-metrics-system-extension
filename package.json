{
  "name": "chrome-metrics-changer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/content.js --bundle --format=iife --outfile=content.js",
    "test": "node --test",
    "clean": "rm -rf dist/",
    "package": "npm run build && npm test && npm run clean && mkdir -p dist/staging/icons && cp manifest.json content.js background.js popup.html popup.css popup.js dist/staging/ && cp icons/icon16.png icons/icon48.png icons/icon128.png dist/staging/icons/ && cd dist/staging && zip -r ../amazon-imperial-to-metric.zip . && cd ../.. && rm -rf dist/staging && echo 'Package created: dist/amazon-imperial-to-metric.zip'"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "esbuild": "0.25.12",
    "eslint": "10.0.0",
    "globals": "17.3.0",
    "prettier": "3.8.1",
    "puppeteer": "24.37.3"
  }
}
